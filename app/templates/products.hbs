{{page-title "Products"}}

<div class="mt-[72px] max-w-[1440px] h-screen mx-auto px-[136px]">
   
<ItemNav/>
    
<PageHeader @heading="Groceries" 
  @subheading="Special FX offers only the finest organic produce." />
  
  <div class="max-w-[1168px]">
    <div class="grid mt-8 2xl:grid-cols-3 xl:grid-cols-3 lg:grid-cols-2 sm:grid-cols-1 xs:grid-cols-1 gap-14">
      {{#each this.model as |product|}}
        <Product @product={{product}} />
      {{/each}}
    </div>  
  </div>
</div>

